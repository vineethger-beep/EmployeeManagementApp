spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/employee_manager
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        jdbc:
          batch_size: 20
